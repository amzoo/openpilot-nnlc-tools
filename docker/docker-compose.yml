services:
  tools:
    build:
      context: ..
    volumes:
      - ../data:/app/data
      - ../output:/app/output

  train:
    build:
      context: ..
    volumes:
      - ../data:/app/data
      - ../output:/app/output
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
